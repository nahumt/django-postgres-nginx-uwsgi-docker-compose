version: '3.7'
services:
  app:
    image: nahumt/respirar:base
    env_file:
      - ./env/app/.base.env
    depends_on:
      - db
      - cache
  db:
    image: postgres:12.0-alpine
    env_file:
      - ./env/db/.base.env
  cache:
    image: redis:latest
volumes:
  postgres_data:
